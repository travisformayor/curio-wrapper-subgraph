{
  "name": "curio-wrapper",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ travisformayor/curio-wrapper",
    "create-local": "graph create --node http://localhost:8020/ travisformayor/curio-wrapper",
    "remove-local": "graph remove --node http://localhost:8020/ travisformayor/curio-wrapper",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 travisformayor/curio-wrapper"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
